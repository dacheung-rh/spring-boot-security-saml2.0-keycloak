server:
  port: 8081
  servlet:
    context-path: /
spring:
  main:
    allow-circular-references: true

keycloak:
  client: spring-boot-keycloak

saml:
  keystore:
    path: classpath:/saml/keystore.jks
    password: store123
    alias: spring-boot-keycloak
  url:
    descriptor: https://keycloak.apps.sno.cheers.local/realms/IDP_REALM/protocol/saml/descriptor

